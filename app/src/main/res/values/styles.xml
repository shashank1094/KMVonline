<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="textAppearanceSearchResultTitle">@style/mySearchResult</item>
    </style>

    <style name="mySearchResult" parent="TextAppearance.AppCompat.SearchResult.Title">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:backgroundDimAmount">@color/appWhite</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Dark.Dialog" parent="Theme.AppCompat.Dialog">
        <item name="colorAccent">@color/appWhite</item>
        <item name="android:textColorPrimary">@color/appWhite</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="Theme.MyTheme" parent="Theme.AppCompat">
        <item name="searchViewStyle">@style/MySearchViewStyle</item>
    </style>
    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Background for the search query section (e.g. EditText) -->
        <item name="queryBackground">@color/appBlue</item>
        <!-- Background for the actions section (e.g. voice, submit) -->
        <item name="submitBackground">@color/appBlue</item>
        <!-- Close button icon -->
        <item name="closeIcon">@color/appBlue</item><!-- Search button icon -->
        <item name="searchIcon">@color/appBlue</item><!-- Go/commit button icon -->
        <item name="goIcon">@color/appBlue</item>
        <!-- Voice search button icon -->
        <item name="voiceIcon">@color/appBlue</item>
        <!-- Commit icon shown in the query suggestion row -->
        <item name="commitIcon">@color/appBlue</item>
        <!-- Layout for query suggestion rows -->
        <item name="suggestionRowLayout">@color/appBlue</item>
    </style>

</resources>
